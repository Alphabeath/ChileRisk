services:
  frontend:
    build: ./frontend
    ports:
      - "${FRONTEND_HOST_PORT:-3000}:3000"
    environment:
      NEXT_PUBLIC_API_BASE: http://localhost:8000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  backend:
    build: ./backend
    ports:
      - "${BACKEND_HOST_PORT:-8000}:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://chilerisk:chilerisk@db:5432/chilerisk
      BACKEND_CORS_ORIGINS: '["http://localhost:3000","http://localhost:3001"]'
      ENABLE_SCHEDULER: "true"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      # Python-based healthcheck (no extra packages needed in slim image)
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status == 200 else 1)"]
      interval: 15s
      timeout: 8s
      retries: 6
      start_period: 25s

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: chilerisk
      POSTGRES_USER: chilerisk
      POSTGRES_PASSWORD: chilerisk
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      # PostgreSQL host port (loaded from .env or defaults to 5434)
      # Edit .env to change: POSTGRES_HOST_PORT=5435
      - "127.0.0.1:${POSTGRES_HOST_PORT:-5434}:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chilerisk -d chilerisk"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  pgdata:
